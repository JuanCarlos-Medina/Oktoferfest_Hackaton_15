<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_2"
    android:id="@+id/noSending">

    <TextView
        android:id="@+id/textViewNoSending"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="You cannot \n send vouchers."
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/the_button"
        android:textColor="@color/color_text"
        android:layout_alignParentTop="true"
        android:gravity="center"/>

    <Button
        android:id="@+id/the_button"
        android:background="@color/color_5"
        style="@style/ElementMargins"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="I am a shop and/or have vouchers and want to sign up."
        android:onClick="registerDeveloperOnClickListener"
        android:gravity="center"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

</RelativeLayout>